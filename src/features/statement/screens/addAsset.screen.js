import {BackgroundView} from "../../../infrastructure/navigation/index.styles";
import {AddAssetTitle, ScreenTitle} from "./statement.styles";

export const AddAssetScreen = () => {
    return (
        <BackgroundView>
            <ScreenTitle>
                ADICIONAR ATIVO
            </ScreenTitle>
            {/*<SelectAssetType/>*/}
            {/*tipo*/}
        </BackgroundView>
    )
}